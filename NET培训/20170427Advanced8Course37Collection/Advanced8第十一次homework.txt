1 完成上一次的作业

2 做个用户登陆，可以按照课程中用的，然后再升级为ajax版本的，使用AuthorityFilter检验用户登陆，做个权限检测（用户和菜单的映射可以自己插入数据库）

3 在UserController增加一个功能，给用户绑定一系列的菜单(界面不要求，实现后台逻辑即可)，然后在layout的上方，根据当前用户的权限去展示拥有  权限的菜单(菜单链接不需要实现)

4 通过自定义的IHttpModule完成对请求的记录和时间统计，要求写文本日志(log4或者自建log)，
  如果碰到首页(Home/Index)的话，在页面底部增加一个输出“当前网站的作者是谁，花了多长ms打开了这个页面”(注释的内容，不要影响UI)

5 通过自定义的IHttpHandler完成对ajax请求的处理，ajax的请求的url地址要以.ajax做后缀，
  通过自定义的IHttpHandler完成对.readme后缀的请求，返回自定义的系统说明；

6 可选任务：试试通过缓存记录用户对.readme后缀的请求，要求1分钟内只能访问10次，超过了则跳转到验证码页面，填写正确了才可以继续访问

7 自定义一个特殊路由，如果访问的浏览器是IE10以下，直接拒绝访问；
  自定义一个IRouteHandler，通过访问/MVCVersion/{任意信息}，能返回当前版本信息等

8 扩展razor引擎，根据不同的浏览器，展示不同的一套view；
                 





作业要求在下周五(12.16)晚上22点前发到邮箱：57265177@qq.com
作业用压缩包的形式发给我，压缩包名称命名以群里的昵称开头，然后有更新的话，在昵称后面加上_update1这样子
第一次提交是：   24一两_homework1.rar
第二次提交是：   24一两_homework1_Update1.rar
第三次提交是：   24一两_homework1_Update2.rar

压缩包中希望包含作业说明文档，可以是完成过程中的问题、思路、解决方案、学习感悟、笔记都可以了，，
如果作业有更新，则必须把更新的内容写入文档，没有更新文档的拒绝批改(:


然后越早交作业的  老师会单独批改，一对一指导，超时交的作业可能没法即时批改。。。